'use strict';

var _arithmetic_module = require('./arithmetic_module');

var result = _arithmetic_module.operations.add(6, 10);

// Consume the Arithmetic Module

console.log(result);

var diff = _arithmetic_module.operations.sub(39, 10);
console.log(diff);